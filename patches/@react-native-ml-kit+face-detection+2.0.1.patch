diff --git a/node_modules/@react-native-ml-kit/face-detection/RNMLKitFaceDetection.podspec b/node_modules/@react-native-ml-kit/face-detection/RNMLKitFaceDetection.podspec
index c23f11e..ef7969a 100644
--- a/node_modules/@react-native-ml-kit/face-detection/RNMLKitFaceDetection.podspec
+++ b/node_modules/@react-native-ml-kit/face-detection/RNMLKitFaceDetection.podspec
@@ -20,7 +20,11 @@ Pod::Spec.new do |s|
   s.source_files = "ios/**/*.{h,c,cc,cpp,m,mm,swift}"
   s.requires_arc = true
 
-  s.dependency "React"
+  if respond_to?(:install_modules_dependencies, true)
+    install_modules_dependencies(s)
+  else
+    s.dependency "React-Core"
+  end
   s.dependency "GoogleMLKit/FaceDetection", "8.0.0"
 end
 
diff --git a/node_modules/@react-native-ml-kit/face-detection/ios/FaceDetection.m b/node_modules/@react-native-ml-kit/face-detection/ios/FaceDetection.m
index 87fd4ff..52594c3 100644
--- a/node_modules/@react-native-ml-kit/face-detection/ios/FaceDetection.m
+++ b/node_modules/@react-native-ml-kit/face-detection/ios/FaceDetection.m
@@ -240,8 +240,23 @@ RCT_EXPORT_METHOD(detect: (nonnull NSString*)url
                   rejecter:(RCTPromiseRejectBlock)reject)
 {
     NSURL *_url = [NSURL URLWithString:url];
-    NSData *imageData = [NSData dataWithContentsOfURL:_url];
-    UIImage *image = [UIImage imageWithData:imageData];
+    NSData *imageData = nil;
+    UIImage *image = nil;
+
+    if ([url hasPrefix:@"file://"]) {
+        image = [UIImage imageWithContentsOfFile:[url substringFromIndex:7]];
+    } else if ([url hasPrefix:@"/"]) {
+        image = [UIImage imageWithContentsOfFile:url];
+    } else if (_url != nil) {
+        imageData = [NSData dataWithContentsOfURL:_url];
+        image = [UIImage imageWithData:imageData];
+    }
+
+    if (image == nil) {
+        reject(@"Face Detection", @"Could not load image for face detection", nil);
+        return;
+    }
+
     MLKVisionImage *visionImage = [[MLKVisionImage alloc] initWithImage:image];
     MLKFaceDetectorOptions *options = [self getOptions:optionsDict];
     MLKFaceDetector *faceDetector = [MLKFaceDetector faceDetectorWithOptions:options];
